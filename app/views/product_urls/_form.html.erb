<%= form_with(model: product_url, local: true) do |form| %>
  <% if product_url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product_url.errors.count, "error") %> prohibited this product_url from being saved:</h2>

      <ul>
        <% product_url.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_area :price %>
  </div>

  <div class="field">
    <%= form.label :blog_id %>
    <%= form.text_field :blog_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
